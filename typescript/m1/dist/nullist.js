"use strict";
{
    const age = 18;
    if (age >= 18) {
        console.log("this is ager");
    }
    else {
        console.log("no ager ");
    }
    const add = {
        name: "hridoy",
        address: {
            thana: "matlab"
        },
        mobile: 5636
    };
    const isAuth = null;
    const result = isAuth !== null && isAuth !== void 0 ? isAuth : "huaifjoij";
    const result2 = isAuth ? isAuth : "guest";
    console.log("this is nullist: ", { result }, { result2 });
}
